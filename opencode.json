{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "loom": {
      "type": "local",
      "command": ["uv", "run", "--directory", "platform", "python", "mcp_server.py"],
      "enabled": true
    }
  }
}
